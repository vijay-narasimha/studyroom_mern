# Frontend Environment Variables

Create a `.env` file in the frontend directory with the following variables:

- `REACT_APP_SERVER_URL`: Base URL for the backend API (e.g., `http://localhost:5000`)
- `REACT_APP_WS`: WebSocket server domain for real-time communication (e.g., `localhost:5000`)
